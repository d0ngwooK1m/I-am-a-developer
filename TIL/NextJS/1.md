# Next js 강좌 #1 소개, 페이지 레이아웃 (Intro, Page layout)

### 1. yarn create next-app (프로젝트명) 으로 생성할 수 있다.

- 컴파일과 번들링이 자동으로 된다 (webpack과 babel)
- 자동 리프레쉬 기능으로 수정하면 화면에 바로 반영된다. (이 부분은 수정 후 잘 작동 안하는 듯? 나중에 알아보자)
- 서버사이드 렌더링이 지원된다.
- 스태틱 파일을 지원한다.

### 2. \_app.js에서 직접 만든 컴포넌트를 활용할 수 있다.

- 페이지 전환시 레이아웃을 유지할 수 있다.
- 페이지 전환시 상태값을 유지할 수 있다.
- componentDidCatch를 이용해서 커스텀 에러 핸들링을 할 수 있다.
- 추가적인 데이터를 페이지로 주입시켜주는게 가능하다.
- 글로벌 CSS를 이곳에 선언한다.

### 3. \_document.js 는 마크업을 커스텀하는데 사용할 수 있다. HTML, Body, Head 등을 만지는데 사용된다. => Next.JS는 마크업 정의를 건너뛰기 때문에

### 4. 컴포넌트는 semantic-ui-react로, style은 inline 스타일링으로
